# Frontend environment configuration — copy to .env and fill in real values
# Leave blank to use the dev proxy in setupProxy.js (/api -> localhost:3000)
REACT_APP_API_URL=
REACT_APP_SOCKET_URL=
CI=false
